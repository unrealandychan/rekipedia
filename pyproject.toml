[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rekipedia"
version = "0.9.11"
description = "Agentic repo-to-wiki: scan any repository into a knowledge store with wiki pages, diagrams, and grounded Q&A."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["wiki", "documentation", "llm", "knowledge-base", "agent"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Documentation",
    "Topic :: Software Development :: Documentation",
]
dependencies = [
    "certifi",
    "curl_cffi>=0.7",
    "click>=8.1",
    "pydantic>=2.0",
    # pyturso provides a modern MVCC storage engine.
    # Falls back to stdlib sqlite3 automatically on platforms without a wheel.
    "pyturso>=0.5; platform_machine != 'aarch64'",
    "rich>=13.0",
    "litellm>=1.30",
    "pyyaml>=6.0",
    "pathspec>=0.12",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "jinja2>=3.1",
    "markdown>=3.6",
    "python-multipart>=0.0.9",
    "tqdm>=4.67.3",
    "pyfiglet>=1.0.4",
    "numpy>=1.26",
]

[project.optional-dependencies]
rag = [
    "faiss-cpu>=1.7",
    "numpy>=1.26",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[project.scripts]
rekipedia = "rekipedia.cli:main"
reki = "rekipedia.cli:main"

[project.urls]
Homepage = "https://github.com/unrealandychan/rekipedia"
Repository = "https://github.com/unrealandychan/rekipedia"
Issues = "https://github.com/unrealandychan/rekipedia/issues"
Changelog = "https://github.com/unrealandychan/rekipedia/blob/main/RELEASE-NOTES.md"

[tool.hatch.build.targets.wheel]
packages = ["src/rekipedia"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "sqlite-utils>=3.38",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"

[tool.coverage.run]
source = ["rekipedia"]
branch = true
omit = [
    "src/rekipedia/sandbox/tasks/analyze_shard.py",  # Docker entrypoint, not importable in tests
    "src/rekipedia/__main__.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
fail_under = 74
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.coverage.html]
directory = "htmlcov"
